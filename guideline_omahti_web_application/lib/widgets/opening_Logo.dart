import 'package:flutter/material.dart';

class OpeningLogo extends StatelessWidget {
  const OpeningLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}